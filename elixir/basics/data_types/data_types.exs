IO.puts("Hello, world!")

# Basic types in elixir
a = 1          # integer
b = 0x1F       # integer
c = 1.0        # float
d = true       # boolean
e = :atom      # atom / symbol
f = "elixir"   # string
g = [1, 2, 3]  # list
h = {1, 2, 3}  # tuple
