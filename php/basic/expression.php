<?php

// Expression is sth that has a value or evaluates to a value
$x = 5;     // anything after the assignment operator is an expression
$y = $x;    // $y evaluates to 5
$z = $x === $y;

if ($x <5) { // "$x <5" is an expression
    echo "Hello";
}
