[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
utils = { path = "../utils" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "benchmarks"
harness = false
